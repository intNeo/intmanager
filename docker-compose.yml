services:
  dsbotintmanager:
    image: intneo/dsbotintmanager:1.2.0
    container_name: dsbotintmanager
    restart: unless-stopped

    environment:
      ENV_FILE: /app/config/.env

    volumes:
      - ./config:/app/config
      - ./data:/app/data